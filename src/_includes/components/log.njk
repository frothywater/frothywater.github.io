<article id="{{ log.basename | slug }}" class="log card">
  {{ log.content | md | safe }}

  <time datetime="{{ log.date | date('DATETIME') }}">
      <a href="/log/#{{ log.basename | slug }}" class="bare-link">{{ log.date | date('yyyy-MM-dd') }}</a>
  </time>
</article>